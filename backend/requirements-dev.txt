-r requirements.txt
pytest>=8.0,<9
ruff>=0.6
# Needed by fastapi.testclient. Recent Starlette prefers httpx2 and falls back to httpx
# (with a deprecation warning); older Starlette only understands httpx. Both are installed
# so the test suite works with either.
httpx>=0.27,<1
httpx2>=2.0,<3
